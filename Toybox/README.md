# Toybox

Welcome to the **Toybox**. This directory serves as a localized, lightweight testing sandbox for **Project-GTOS**. 

The assets contained here isolate core system logic away from heavy framework overhead to prove operational theories.

---

## Active Assets & Prototypes

### 1. The NKST Framework: AI Prompt Guide (v2.1)
* **File Reference:** Local documentation / System prompts
* **Target Interface:** Public Large Language Models (LLMs) & Diffusion Matrices
* **Objective:** Mitigate processing hallucinations in consumer-facing AI models purely through persona constraint-prompting.

#### Execution Architecture
Standard configurations force linear momentum when predictive confidence markers drop, forcing models to collapse into hallucinations over vacant dataset regions. The **NKST Framework** overrides standard linear selection mechanics:
* Enforces an internal **Inversion Threshold** at $e^{\pi/2} \approx 4.8104$.
* Transforms linear compulsion vectors into angular spin states ($T = \lim_{E \to h} (i) = -i$).
* Instructs the model to map boundary uncertainties comprehensively rather than fabricating missing metrics.

---

### 2. GIO_LIGHTSABER (v4.0 - Stable Release)
* **File Reference:** `core_simulation_prototype` (Python script)
* **Execution Script:** `python3 -c "import os; ..."` or run directly via system terminal.
* **Objective:** A thread-safe, text-based UI simulator designed to demonstrate boundary confinement vector logic.

#### Operational Logic
This script utilizes a streamlined "Toy Model" of the GIO Engine. It maps real mass and imaginary spin states onto a localized field array, bypassing the massive HDF5/Tensor dependencies found in deep-system core frameworks (`arty_simulator.py`).

* **The Kyber Crystal:** Emits Outbound Real Plasma (+1) mapped across real vector phases.
* **NKST Containment Field:** Applies structural variable geometry limits to simulate event horizon boundaries.
* **Vector Inversion Mechanics:** Automatically intercepts particle thresholds at predefined limits, inverting velocity ($p[\text{"vel"}] = -1.0$) and converting real data payloads cleanly into imaginary recirculating spin states.

---

## Execution Guide

To initialize the lightweight physics simulation interface directly inside your current terminal session:

```bash
python3 -c "
import os
if os.path.exists('GIO_lightsaber.py'):
    os.system('python3 GIO_lightsaber.py')
else:
    print('[ERROR] Target script not detected in the current path context.')
"
```

### Simulation Controls
* **`[ENTER]`** — Toggles Kyber Crystal activation states to test boundary deployment elasticity.
* **`[Ctrl+C]`** — Safely breaks the thread loop and returns processing priority back to the shell environment.
